<manifest xmlns:android="http://schemas.android.com/apk/res/android">
<uses-permission android:name="android.permission.CAMERA"/>
<uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
<uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
<uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:label="picgallery"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:resizeableActivity="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <!-- Tells Android this activity properly handles runtime window-size
                 changes (fold/unfold, split-screen, drag-resize on large screens)
                 via configChanges above, instead of being restarted or dropped
                 into Size-Compatibility Mode. Without this, foldables like the
                 Galaxy Z Fold can letterbox the app into its last known (folded)
                 window size when unfolded, leaving the rest of the screen blank —
                 which is what was happening on the login screen. -->
            <meta-data
              android:name="android.supports_size_changes"
              android:value="true" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!--
         WRITE_EXTERNAL_STORAGE needed by `gal` for saving to a custom album
         on Android 9 (API 28) and below.  On Android 10+ (API 29+) the
         framework treats this permission as a no-op; scoped storage handles
         MediaStore access without it.
    -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin.

         Also includes the `upi` scheme intent required by the `upi_india` package
         to detect installed UPI apps (GPay/PhonePe/Paytm/BHIM) at runtime under
         Android 11+ (API 30+) package visibility restrictions.

         Also includes an `https` VIEW intent required by the `url_launcher` package
         so `canLaunchUrl`/`launchUrl` can find a browser to open external website
         links (e.g. the subscription redirect to picgallery.in) under Android 11+
         (API 30+) package visibility restrictions. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="upi" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>