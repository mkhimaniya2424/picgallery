fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
psycopg[binary]==3.2.13
alembic==1.14.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.20
email-validator==2.2.0
bcrypt==4.0.1
google-auth==2.37.0
requests==2.32.3
Pillow==11.1.0
firebase-admin==6.6.0

# --- Cloud storage (Cloudflare R2 / any S3-compatible endpoint) ---
boto3==1.35.99

# --- Face recognition (Task: Face Search) ---
# insightface's buffalo_l model produces 512-D ArcFace embeddings; these
# are stored in Postgres via pgvector and compared with cosine distance.
# opencv-python-headless (not opencv-python) since this runs on a
# server with no display/GUI libs available.
numpy==1.26.4
opencv-python-headless==4.10.0.84
onnxruntime==1.19.2
#insightface==0.7.3
pgvector==0.3.6
